# Generics v1 Implementation Plan

> executing-plans. No commit. No version bump. **Forbidden paths:** `src/parser/**`, `src/backend/**`.

**Spec:** `docs/superpowers/specs/2026-09-06-generics-v1-design.md`

If `ClassDef` has no `type_params` yet, implement `Type::App` + `from_str` tests first, then poll `src/parser/class/def.rs` and finish bind/instantiate.

- [ ] `Type::App`, `from_str` nested, Display, `is_resource`; delete blanket `reject_fake_generics`.
- [ ] Registry: generic class templates vs concrete `List__int`.
- [ ] Substitute + inject concrete class before MIR (`src/compiler/pipeline/` and/or `src/types/checker`).
- [ ] Tests: `flock … cargo test --offline --lib types:: -- --test-threads=1` and `--test type_check_tests`.

Grep `ClassDef {` in `src/types` and tests you own; add `type_params: vec![]` if the parser agent already added the field.
